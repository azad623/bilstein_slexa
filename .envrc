#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"

# Activate the conda environment
eval "$(conda shell.bash activate "bilstein_slexa")"
[ "$CONDA_DEFAULT_ENV" = "bilstein_slexa" ] || echo "Have you run \`make install\`?"
